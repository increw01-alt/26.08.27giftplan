# 레퍼런스 기반 화면 사용 규칙

## 프레임

- 데스크톱 콘텐츠 최대 너비: `1152px`
- 세부페이지 본문 최대 너비: `960px`
- 헤더 높이: `64px`
- 메인 히어로: 최소 `660px`, 화면 높이가 더 크면 `100svh`
- 세부페이지 히어로: `510px`
- 기본 섹션 상하 여백: `96px`
- 태블릿 전환점: `1040px`
- 모바일 전환점: `760px`

## 글꼴과 위계

- 기본 글꼴: `Paperlogy`, 없으면 `Pretendard`, `Noto Sans KR` 순서로 사용
- 메인 히어로 제목: `40–54px`, 굵기 `850`, 행간 `1.19`
- 세부페이지 제목: `38–52px`
- 섹션 제목: `30–42px`
- 본문: `16px`, 행간 약 `1.68–1.75`
- 작은 분류문구: `12–13px`, 영문은 넓은 자간 사용

## 색상과 표면

- 기본 네이비: `#071a2d`
- 섹션 네이비: `#102c46`
- 주요 블루: `#168cf5`
- 강조 코랄: `#ff3f64`
- 강조 터쿼이즈: `#17c5c9`
- 페이지 배경: `#f7f8f6`
- 세부 본문 배경: `#f5f7fb`
- 카드 모서리: `18–20px`
- 히어로 겹침 패널 모서리: `28px`

## 이미지와 텍스트 배치

- 메인 히어로는 선명화한 `hero-gift-cards-main-v4-sharp.png`를 전체 배경으로 사용한다.
- 배경 이미지는 `cover`로 채우고 텍스트는 1152px 프레임의 왼쪽에 정렬한다.
- 텍스트 가독성을 위해 왼쪽이 짙은 수평 그라데이션과 하단 음영을 이미지 위에 겹친다.
- 헤더는 히어로 위에서는 투명하고, 스크롤 후에는 흰색 반투명 배경과 블러를 적용한다.

## 모션

- 히어로 이미지는 `1.5s` 동안 살짝 축소되며 나타난다.
- 히어로 문구는 `0.88s` 동안 아래에서 위로 나타나고 요소별로 시차를 둔다.
- Animate.css v4의 CSS 변수·진입 패턴을 참고한 공통 모션 토큰을 메인·세부페이지에서 함께 사용한다.
- 섹션 제목은 화면 진입 시 `34px` 아래에서 `0.82s`, 카드는 `30px` 아래에서 `0.72s` 동안 나타난다.
- 같은 카드 그룹은 `65ms` 간격으로 최대 6개까지 순차 등장한다.
- 카드 호버는 약 `240ms` 동안 위로 `8px` 이동하고 시안 테두리와 그림자를 강화한다.
- 히어로 버튼은 동일한 폭으로 구성하고 호버 시 시안 `#00d9ff` 또는 핑크 `#ff3b5b`로 전환한다.
- 헤더 로고는 투명 헤더에서 어두운 배경용 이미지를, 스크롤 후 흰색 헤더에서 밝은 배경용 이미지를 같은 크기로 전환한다.
- `prefers-reduced-motion` 환경에서는 이동 애니메이션을 제거한다.
